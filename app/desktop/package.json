{
  "name": "desktop-app",
  "version": "1.0.0",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "pack": "electron-packager . --overwrite --platform=win32 --arch=x64 --icon=path/to/your/app-icon.ico --out=dist/",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "electron-start": "electron ."
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "electron-packager": "^17.1.2"
  }
}
